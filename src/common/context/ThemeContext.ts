import React from "react";

export const colorTheme = {
  light: {
    primary: "#fff",
    secondary: "#000101",
    tertiary: "black",
  },

  dark: {
    primary: "#333",
    secondary: "#fff",
    tertiary: "#fff",
  },
};

export const ThemeContext = React.createContext({});
